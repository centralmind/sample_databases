{
    "name": "Gateway Example Codespace",
    "forwardPorts": [9090],
    "portsAttributes": {
      "9090": {
        "label": "App Port",
        "onAutoForward": "openPreview"
      }
    },
    "customizations": {
        "vscode": {
          "settings": {
            "terminal.integrated.defaultProfile.linux": "bash"
          },
          "extensions": []
        }
      },
    "postCreateCommand": "bash .devcontainer/setup.sh",
    "postStartCommand": "sleep 2 && code --execute-command workbench.action.terminal.new && sleep 1 && code --execute-command workbench.action.terminal.sendSequence --args {\"text\":\"bash .devcontainer/run.sh\\u000D\"}",
    "containerEnv": {      
      "VAR1": "${VAR1}"
    }
  }
  