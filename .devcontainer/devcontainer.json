{
    "name": "Gateway Example Codespace",
    "forwardPorts": [9090],
    "portsAttributes": {
      "9090": {
        "label": "App Port",
        "onAutoForward": "openPreview"
      }
    },
    "postCreateCommand": "bash .devcontainer/setup.sh",
    "postStartCommand": "code README.md && code -r . && sleep 2 && code --new-window && sleep 1 && code --execute-command workbench.action.terminal.new && sleep 1 && code --execute-command 'workbench.action.terminal.sendSequence' --args {\"text\":\"bash .devcontainer/run.sh\\u000D\"}",
    "containerEnv": {      
      "VAR1": "${VAR1}"
    }
  }
  